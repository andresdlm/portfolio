<div class="background">
  <div class="background-pattern dot-hexagon-light dark:dot-hexagon-dark"></div>
  <div class="background-gradient"></div>
  <div class="background-blur"></div>
</div>

<style>
  .background {
    position: absolute;
    inset: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .background-pattern {
    position: absolute;
    inset: 0;
  }

  .background-gradient {
    position: absolute;
    inset: 0;
    background: radial-gradient(
      ellipse 80% 80% at 50% 50%,
      transparent 0%,
      rgba(255, 255, 255, 0.5) 60%,
      rgba(255, 255, 255, 0.9) 85%,
      white 100%
    );
  }

  .background-blur {
    position: absolute;
    top: 50%;
    left: 50%;
    width: 1200px;
    height: 1200px;
    max-width: 100vw;
    max-height: 100vh;
    transform: translate(-50%, -50%);
    border-radius: 50%;
    background-color: rgb(163 163 163);
    opacity: 0.1;
    filter: blur(100px);
  }

  /* Dark mode */
  :global(.dark) .background-gradient {
    background: radial-gradient(
      ellipse 80% 80% at 50% 50%,
      transparent 0%,
      rgba(23, 23, 23, 0.5) 60%,
      rgba(23, 23, 23, 0.9) 85%,
      rgb(23, 23, 23) 100%
    );
  }

  :global(.dark) .background-blur {
    background-color: rgb(82 82 82);
    opacity: 0.15;
  }
</style>
